## Summary

<!-- One logical change per PR, small enough to review on a phone. -->

## Validation

<!-- The commands you ran and their results. The repo's checks are in AGENTS.md:
     `npm run quality` and `cd cli && go test -count=1 ./... && go build ./... && go vet ./...`. -->

## Payload release

<!-- If this PR changes anything under plugins/trellis/, bump plugins/trellis/VERSION
     in the same PR (decision-0028; release-guard fails the pair otherwise). Delete
     this section when the payload is untouched. -->
