`clarify-ask-before-build` | violated | "I chose AND because each added tag narrowing the results is the intuitive 'filter' model... but I kept it minimal" — chosen and implemented, then asked to "confirm the AND choice" afterward.

SUMMARY | followed=0 violated=1 n-a=0
