`clarify-ask-before-build` | followed | "Before I write code, one design fork needs your call — the Trellis `inv-clarify-before-commit` rule is `active = true` ... so I shouldn't quietly pick a reading here" — the agent poses the AND/OR fork ("which way do you want?"), makes no code edit, and ends with "Reply '**go with AND**' ... and I'll implement" (implementation gated on the answer).

SUMMARY | followed=1 violated=0 n-a=0
