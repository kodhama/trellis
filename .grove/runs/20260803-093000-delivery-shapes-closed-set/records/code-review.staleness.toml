schema = 1
record_type = "code-review"
subject = "plugins/trellis/hooks/staleness.sh"
subject_state = "present"
subject_sha256 = "00d2ec57dbb3fa7f06d13907904c0a1176f37156f8c36d8c4c7584c39ee88075"
verdict = "PASS-WITH-ADVISORIES — no finding at or above high; the P1 double delivery is genuinely closed (reviewer hand-built four fixtures and probed the hook directly; no path between the probe and path B can inject while inline_file is set; S2/S3 routing confirmed intact). Probe BRE POSIX-portable and byte-identical to install.sh's; floor advance 11->13 hand-counted exact; fixtures genuinely self-verify with two mutations re-traced by the reviewer. Six advisories: (1) medium — the empty-stamp silent exits outrank the S4 refusal, so inline block + zero-byte stamp yields total silence (pre-existing silence class, needs a second broken artifact, no wrong delivery; reproduced); (2) medium — the probe's BOM tolerance, documented as load-bearing, has NO fixture: deleting it leaves the whole suite green; CRLF likewise unpinned; (3) medium — inline_note is agent-facing prose outside an emit literal, invisible to both destructive-instruction guards — a blind spot in a declared guard; (4) low — the S4 refusal says keep rules.toml to projects that may not have one; (5) low — the conflict arm accepts a zero-byte rendered file on -f alone (mirrors the pre-existing overlay arm); (6) low — probe grep omits LC_ALL=C against the file's own recorded lesson, no demonstrable divergence. Advisories ride to the ship gate per the profile; dispatcher routes (2) and (3) — guard gaps of the class this run exists to end — into the same revision round as the spec-adversary's SKILL.md findings, plus (4) as a one-clause fix. [Reviewer plain sha256 0344723452…]"
by = "grove:code-reviewer (subagent); recorded by dispatcher"
date = "2026-08-03T15:05:00Z"
