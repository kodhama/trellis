schema = 1
record_type = "conformance"
subject = "plugins/trellis/skills/remove/SKILL.md"
subject_state = "present"
subject_sha256 = "f651b69ea7b6aa7f066d9874854f1c5a7fa4d407353c6e3416403fee67db8f26"
verdict = "[reviewer-reported plain sha256 13d071ae21e3… over the same bytes; digest above is the guard's domain-separated grove:file-bytes tag.] FAIL (fidelity, three minor items) with one co-primary UPSTREAM-INDICTED routed to the decision layer. Upstream established independently: spec-0004 (ratified; its supersession note names this skill its live home) + decision-0068 D11/D12 (approved) + decision-0070 (approved) + decision-0072 (GATED — every sweep-derived item conditional on its intent act). Fidelity FAILs: item 5, no instruction for the both-rollback-markers-absent case, which spec-0004 SS2/AC3 requires ('says it cannot locate a rollback point rather than guess'); item 10, the curl-install illustrating sentence is false against decision-0070 D2 — install.sh seeds rules.toml, so a fresh curl install carries TWO shapes, not one; item 11, the frontmatter description says 'touching nothing else' while the body deletes .claude/rules/trellis.md and edits AGENTS.md per 0068 D11. UPSTREAM-INDICTED (item 12): the skill is FAITHFUL to its written contract, and the contract is wrong — decision-0070 D1/D3/D6 made the project-scope bundle at .claude/skills/trellis/ the adoption act and propagated nothing into the remove contract; a governed-14/14 bundle-only project is reported 'already absent', and a successful remove leaves the bundle as a live adoption signal while install.sh:548 and the plugin README advertise remove as total. Routing: decision layer — the remove contract needs the adoption-shape clause; the skill must NOT be fixed without the upstream amendment (its own 'touch nothing else' forbids it). That amendment is decision-0073 (gated, this run). Gates run by the reviewer: go test ok 15.3s. Evidence mode inferred; no canonical test-deps carrier exists (advisory)."
by = "grove:conformance-reviewer (subagent); recorded by dispatcher"
date = "2026-08-03T10:12:00Z"
