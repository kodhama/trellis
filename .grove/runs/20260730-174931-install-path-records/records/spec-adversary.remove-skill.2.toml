schema = 1
record_type = "spec-adversary"
subject = "plugins/trellis/skills/remove/SKILL.md"
subject_state = "present"
subject_sha256 = "f651b69ea7b6aa7f066d9874854f1c5a7fa4d407353c6e3416403fee67db8f26"
verdict = "[reviewer-reported plain sha256 13d071ae21e3… over the same bytes; digest above is the guard's domain-separated grove:file-bytes tag.] NEEDS-REVISION — intrinsic-quality review of the skill as the effective normative contract (type unclaimed; owed fail-closed). Six load-bearing findings: F1 frontmatter says 'touching nothing else' while the body touches AGENTS.md, three more instruction files, and deletes .claude/rules/trellis.md; F2 the ordering rationale claims every interruption window leaves a delivery path intact, falsified by its own step order (post-step-2 window is the ungoverned state its line 84-86 defines); F3 the M2-morph branch is unreachable from the numbered procedure and step 4.4 deletes .trellis/rollback, the record that branch depends on; F4 the no-op predicate's 'all three installed shapes' enumeration is contradicted by the spec's own fourth (morph) state — open enumeration presented as closed; F5 four provenance predicates ('known to have been added by Trellis' etc.) have no decision procedure, making the consent stop-condition untestable; F6 the exactly-one marker cardinality is ambiguous between per-file and project-wide, and one reading hard-stops a state line 61 declares legitimate. Secondary: F7 verification failure behavior unspecified; F8 'may be removed' nondeterministic; F9 'empty' undefined against the whitespace clause; F10 report taxonomy does not map to the five-file inspection surface; F11 ignore targets enumerated by tool, not file; F12 observation for the fidelity reviewer on externally-defined hook-path claims. Routing: producing layer — consumed by the decision-0073 build stage, which rewrites this subject under D3. Full findings in the reviewer's report, session 2026-08-03."
by = "grove:spec-adversary (subagent); recorded by dispatcher"
date = "2026-08-03T10:05:00Z"
