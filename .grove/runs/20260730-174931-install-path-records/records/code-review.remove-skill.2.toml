schema = 1
record_type = "code-review"
subject = "plugins/trellis/skills/remove/SKILL.md"
subject_state = "present"
subject_sha256 = "f651b69ea7b6aa7f066d9874854f1c5a7fa4d407353c6e3416403fee67db8f26"
verdict = "[reviewer-reported plain sha256 13d071ae21e3… over the same bytes; digest above is the guard's domain-separated grove:file-bytes tag.] BLOCK — two blocking findings, five advisory. F1 (severe): the vendored plugin bundle at .claude/skills/trellis/ is outside the skill's world model entirely; followed literally, removal deletes .trellis/ and next session the bundle's own hook takes path B (scoped_to_project=yes, rows_are_default=yes) and injects the full 14-rule payload — the removal un-does itself, the step-5 report claims completion, every collaborator who clones the committed bundle is re-governed, and a bundle-only project satisfies the 'already absent' predicate while fully governed. Mechanism verified against staleness.sh:362-416. F2 (high): morph detection is ordered AFTER the destructive transaction — .trellis/rollback lives inside the directory step 4.4 deletes, so the standard path destroys the recorded rollback SHA before reaching the section that forbids exactly that; the 'no overlay to strip' discriminator is false for the note-only morph state. F3 (medium): the S4 ordering rationale asserts 'every interruption window leaves at least one delivery path intact', false for the post-step-2 window; the reviewer notes the invariant was achievable with a different order (internal/ before block, rules.toml last, path B delivering throughout). F4: two provenance predicates undecidable from disk (no receipt exists; verified against the decision-0049-era setup which wrote unmarked lines). F5: marker-cardinality scope per-file vs project-wide unstated; the two readings diverge on a legitimate multi-file state. F6: consumer-authored rules.toml rows deleted unsurfaced while lesser artifacts get consent. F7 (low): separator-newline phrasing wrong for manually placed blocks. Positive: hook cross-references verified accurate; transaction shape sound; rendered-before-overlay rationale correct. Disposition: BLOCK on F1+F2, returns to the executor — which is the decision-0073 build stage; F1's route is bundle handling per 0073 D3, F2's is morph detection in preflight before any write."
by = "grove:code-reviewer (subagent); recorded by dispatcher"
date = "2026-08-03T10:20:00Z"
