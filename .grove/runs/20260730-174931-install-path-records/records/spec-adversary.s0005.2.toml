schema = 1
record_type = "spec-adversary"
subject = "specs/0005-curl-install-mechanical-vendoring.md"
subject_state = "present"
subject_sha256 = "008a75ed1723dada5b12ef1a289cb8b4f0f2d8ed72cc453702e43cbc26cbef9d"
verdict = "[reviewer-reported plain sha256 7e736501ac33… over the same bytes; digest above is the guard's domain-separated grove:file-bytes tag.] NEEDS-REVISION — the 0070/0072 partial supersessions were recorded as frontmatter comments and never landed in the body, the exact defect class the spec's own 0066 pointer declares its standard forbids ('a row naming a file that no longer exists is a live spec defect, not archive'). F1 (high): AC10 and two coverage rows mandate a verbatim /trellis:setup next-step that install.sh no longer prints and the CI gate now FORBIDS (install_script_test.go asserts its absence) — an implementer building from the spec fails the test gate. F2 (high): the S1 bundle table still lists skills/setup/SKILL.md, deleted; AC1 ('a fresh run writes every file in S1's table') is literally unsatisfiable; the escape hatch covers growth, not shrinkage. F3 (medium-high): AC2's 'six reads over five paths' normative bound forbids the decision-0070 seed the same frontmatter permits — the live script has three rules.toml read sites. F4 (medium): S2's prompt rule contradicts install.sh's in-repo tty prompt; no amendment on either side — needs an owner ruling (script defect vs unrecorded widening). F5 (medium-low): S3 mandates a two-artifact pin+manifest chain the shipped single-baked-manifest design does not have, half-acknowledged by the 0066 pointer. F6-F8 low/trivial: stale checksums enumeration naming four deleted files; AC2d's column-0-only bound forbids the BOM tolerance the code measured as necessary; AC10 cross-references a S3 prompt that is a non-interactive exit. The surviving normative core (mechanical vendoring, fail-closed integrity, one-content-read, render contract, hook coordination) binds and is implemented — verified positively. Routing: producing layer, an in-place corrective amendment pass of the same shape the spec has used twice; F4 additionally needs a maintainer ruling. Dispatcher note: this is decision-0072 retirement propagation left unlanded — routed to the #227 tail, not the 0073 run."
by = "grove:spec-adversary (subagent); recorded by dispatcher"
date = "2026-08-03T10:20:00Z"
